<UserControl
    x:Class="MyApp1.MyControl.ServerData"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyApp1.MyControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch" Margin="10">
    <UserControl.ContextFlyout>
        <MenuFlyout>
            <MenuFlyout.Items>
                <MenuFlyoutItem Text="删除" Click="MenuFlyoutItem_Click">
                </MenuFlyoutItem>
            </MenuFlyout.Items>
        </MenuFlyout>
    </UserControl.ContextFlyout>
    <Border CornerRadius="5" Background="{ThemeResource  BackButtonPointerOverBackgroundThemeBrush}" Padding="15">
        <Grid >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="{x:Bind MyData.Name}" FontSize="20"/>
                    <TextBlock FontSize="20">
                        <Run Text="["/>
                        <Run Text="{x:Bind MyData.Host}"/>
                        <Run Text="]"/> 
                    </TextBlock>
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="0 10 0 0">
                    <TextBlock>
                        <Run Text="游戏版本:"/>
                        <Run  x:Name="GameVersion"/>
                        <Run Text="       游戏人数:"/>
                        <Run  x:Name="PeopleCount"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="测试链接" Style="{ThemeResource AccentButtonStyle}" Padding="10" Click="Button_Click"/>
                <Button Content="连接" Style="{ThemeResource AccentButtonStyle}" 
                        Margin="10 0 0 0" 
                        Padding="10" 
                        Click="Button_Click_1"/>
                
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
