<ContentDialog
    x:Class="MyApp1.Dialog.StartGameConfig"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyApp1.Dialog"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    CloseButtonText="关闭"
    CloseButtonClick="ContentDialog_CloseButtonClick"
    PrimaryButtonText="添加"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ExePath" PlaceholderText="目标程序"/>
            <TextBox x:Name="ExeArgs" Grid.Column="1" PlaceholderText="目标参数"/>
            <Button Grid.Column="2" Click="Button_Click" Margin="8 0 0 0" Content="浏览" Padding="8"/>
        </Grid>
        <TextBox Margin="0 10 0 0" Grid.Row="1"  x:Name="ExeName" PlaceholderText="名称"/>
        <TextBlock  Margin="0 10 0 0" Grid.Row="2" HorizontalAlignment="Center"  x:Name="Tip" Foreground="Red" FontFamily="等线" FontSize="20"/>
    </Grid>
</ContentDialog>
